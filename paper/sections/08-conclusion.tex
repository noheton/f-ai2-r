\section{Conclusion}\label{sec:conclusion}
% Principal structure:
%
% P1  Recap: F(AI)2R generalized to aiprov; method made executable as a
%     skill; demonstrated on its own production.
% P2  The stance carried over: the repository IS the paper and IS the
%     process — plus: the method that produced both is itself versioned,
%     validated, and shipped in the repository.
% P2b Closing echo of the three roots: from AI-assisted research
%     practice (transcript-as-artifact), engineering provenance
%     (traceability under uncertainty), and industrial semantics
%     (AAS: formal semantics where exchange matters) — aiprov is where
%     they converge; where semantics are important, provenance is the
%     semantics of work.
% P3  Outlook — from method to infrastructure: the designated
%     integration path is shepard \cite{shepard}, a jointly developed
%     DLR system for heterogeneous product and research data; an
%     experimental fork \cite{shepard-fork} serves as the staging
%     ground for systematically integrating aiprov into that technical
%     solution (planned work — the fork is a development workspace with
%     experimental v2 endpoints). Further: multi-agent attribution,
%     energy accounting, registry integration (ORCID/DOI/RAiD),
%     vendoring sources at rung 4 by default, and a combined profile
%     with uncertainty-aware provenance \cite{uncertainty-prov2026}.
